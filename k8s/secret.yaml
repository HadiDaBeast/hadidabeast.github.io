# IMPORTANT: This file contains placeholder values.
# In production, use 'kubectl create secret' or a secrets manager.
# Never commit real credentials to version control.
#
# To create the real secret:
#   kubectl create secret generic prispulsen-secrets \
#     --namespace prispulsen \
#     --from-literal=POSTGRES_USER=prispulsen \
#     --from-literal=POSTGRES_PASSWORD=CHANGEME \
#     --from-literal=DATABASE_URL='postgresql://prispulsen:CHANGEME@postgres:5432/prispulsen'
apiVersion: v1
kind: Secret
metadata:
  name: prispulsen-secrets
  namespace: prispulsen
type: Opaque
stringData:
  POSTGRES_USER: prispulsen
  POSTGRES_PASSWORD: CHANGEME_REPLACE_BEFORE_DEPLOY
  DATABASE_URL: postgresql://prispulsen:CHANGEME_REPLACE_BEFORE_DEPLOY@postgres:5432/prispulsen
